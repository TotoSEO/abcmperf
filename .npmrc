# Payload 3 déclare un peer range strict sur Next ; notre version de Next
# fonctionne mais tombe hors de ce range. On aligne l'installation (Vercel
# comme en local) sur le même comportement pour éviter l'échec ERESOLVE.
legacy-peer-deps=true
