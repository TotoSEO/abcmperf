Styled native select with a custom chevron. Pass `options` or `<option>` children.

```jsx
<Select label="Service" placeholder="Choisir…" options={["Site web","SEO","Community Management"]} />
```

Props: `label`, `options` (strings or `{value,label}`), `placeholder`. Forwards native select attrs.
